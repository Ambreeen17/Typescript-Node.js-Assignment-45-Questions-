//save the name in variable
var namePerson = "maryum";
console.log(namePerson.toLowerCase()); //result will be in lower case "maryum"
console.log(namePerson.toUpperCase()); //result will be in upper case "MARYUM"
console.log(namePerson.charAt(0).toUpperCase() + namePerson.slice(1).toLowerCase()); //result will be in title case "Maryum"
