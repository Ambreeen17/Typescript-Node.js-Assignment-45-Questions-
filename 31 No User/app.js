var user_names = [];
if (user_names.length === 0) {
    console.log("We need to find some users!");
}
else {
    //Greet users
    console.log;
}
// removing all user names ensures the message "we need to find some users!" is printed
